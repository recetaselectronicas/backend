const dataTypes = {
  number: 'number',
  string: 'string',
  boolean: 'boolean',
  code: 'code',
  typed: 'typed'
}

const operatorType = {
  unary: 'unary',
  binary: 'binary'
}

const argumentType = {
  single: 'single',
  list: 'list'
}

module.exports = {
  dataTypes,
  operatorType,
  argumentType
}